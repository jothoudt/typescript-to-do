import React from 'react';

function Header(){
    return(
        <div>
            <input type="text" placeholder="Task" />
            <button>Add Task</button>
        </div>
    )
}

export default Header;